import { NodeTypes } from "../ast";

export function transformTxet(node,contexts) {//我希望将多个子节点拼在一起
if(node.type ===NodeTypes.ELEMENT || node.type ===NodeTypes.ROOT){
    return ()=>{
        
    }
}


}